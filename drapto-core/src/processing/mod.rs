//! Module for specific video processing steps like film grain optimization.

pub mod film_grain; // Keep pub, already correct

// Conditional re-export removed as unit tests are now inline in film_grain.rs