// drapto-core/tests/mod.rs

// Declare the integration test modules.
// Cargo will automatically discover files in the tests/ directory
// if a tests/mod.rs exists.

// Optionally declare modules if needed for organization, but
// Cargo typically finds files like `tests/utils_tests.rs` automatically.
// Let's keep it empty for now and rely on auto-discovery.