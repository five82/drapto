// drapto-cli/src/commands/mod.rs
//
// Declares the submodules for different CLI commands.

pub mod encode; // Declare the encode command module
// Add other command modules here later (e.g., pub mod analyze;)